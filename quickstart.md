# クイックスタートガイド

このドキュメントでは、GUIファイラー（ファイルマネージャー）のセットアップから起動、基本的な使い方までを素早く解説します。

## 1. 動作環境

*   **OS**: Windows 10/11, macOS 13+, Linux
*   **Python**: 3.13 (64bit推奨)

## 2. セットアップ

以下の手順で開発環境を構築し、アプリケーションを起動できるようにします。

### ステップ 1: リポジトリのクローン

```bash
git clone <repository-url>
cd file_manager
```

### ステップ 2: 仮想環境の作成と有効化

**Windows (PowerShell)**
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

**macOS / Linux**
```bash
python3 -m venv .venv
source .venv/bin/activate
```

### ステップ 3: 依存パッケージのインストール

基本機能を動作させるために必要なパッケージをインストールします。

```bash
pip install -r requirements.txt
```

#### (オプション) 拡張機能の有効化
動画のダイジェスト表示や、ゴミ箱への移動機能を使いたい場合は、追加で以下をインストールしてください。

```bash
# 動画ダイジェスト機能（OpenCV）
pip install opencv-python numpy

# ゴミ箱機能
# Windowsの場合
pip install winshell
# macOS/Linuxの場合
pip install send2trash
```

## 3. アプリケーションの起動

仮想環境が有効な状態で、以下のコマンドを実行します。

```bash
python run.py
```

## 4. 基本的な使い方

起動すると、エクスプローラーのような2ペイン型の画面が表示されます。

*   **フォルダ移動**: 左側のツリーまたは右側のリストをダブルクリックして移動します。
*   **表示切替**: ツールバーで「リスト」「アイコン」「詳細」表示を切り替えられます。
*   **ファイル操作**: 右クリックメニューから、コピー、移動、名前変更、削除などが可能です。
*   **検索**: ツールバーの検索ボックスで、現在のフォルダ内のファイルを絞り込み検索できます。

## 5. 便利な機能

このファイラーには、標準的なファイル操作以外にも便利な機能が搭載されています。

*   **動画ダイジェスト**: 動画ファイルを選択すると、内容のダイジェスト（サムネイル）を表示します（要OpenCV）。
*   **重複動画の検出**: ツールバーの「重複動画」ボタンから、サイズや名前が似ている動画を検索・整理できます。
*   **ディスク使用量分析**: 「📊」ボタンを押すと、フォルダごとの容量を円グラフで可視化できます。
*   **インデックス検索**: 「🔍」ボタンから、インデックスを作成して高速な全文検索が行えます。

---
より詳細な設定や機能については、[README.md](./README.md) や [howtouse.md](./howtouse.md) を参照してください。
