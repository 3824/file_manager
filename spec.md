# 開発仕様書 (Specification)

## 1. プロジェクト基本情報
*   **名称**: Cross-Platform File Manager
*   **目的**: Windows/macOS/Linuxで動作する、動画管理機能に強みを持つ高機能ファイラーの開発。
*   **主要技術スタック**:
    *   言語: Python 3.13
    *   GUIフレームワーク: PySide6 (Qt for Python)
    *   動画処理: OpenCV (opencv-python)
    *   システム操作: Standard Library (pathlib, shutil, os), winshell/send2trash (ゴミ箱連携)

## 2. システムアーキテクチャ
*   **デザインパターン**: MVC (Model-View-Controller)
    *   **Model**: `QFileSystemModel` を継承・拡張し、動画メタデータやカスタムカラムに対応。
    *   **View**: `QTreeView` (ツリー), `QListView`/`QTableView` (ファイル一覧) をカスタマイズしてレンダリング。
    *   **Controller/Logic**: メインアプリケーションロジックにてイベントハンドリング、設定管理、非同期タスク管理を行う。
*   **非同期処理**:
    *   重い処理（サムネイル生成、動画解析、ファイル検索）は `QThread` または `QThreadPool` を用いてバックグラウンド実行。
    *   UIスレッド（メインスレッド）をブロックしない設計を徹底する。

## 3. 機能仕様詳細

### 3.1 ユーザーインターフェース (UI)

#### A. ヘッダー領域
1.  **ドライブ/ボリュームバー**
    *   システム上の有効なドライブ/マウントポイントを自動検出し、アイコンボタンとして列挙。
    *   各ボタンに空き容量/使用率を示すインジケータ（プログレスバー等）を表示可能にする。
    *   クリックアクション: 対象ドライブのルートへナビゲート。
2.  **ナビゲーションコントロール**
    *   [戻る] [進む] [上へ] ボタン。
    *   履歴スタックに基づき有効/無効状態を切り替え。
3.  **アドレスバー**
    *   現在のパスをテキストで表示・編集可能。
    *   Enterキーで入力パスへ移動。
4.  **検索ボックス**
    *   カレントディレクトリ配下の検索キーワード入力欄。

#### B. 左ペイン：ディレクトリツリー
*   **コンポーネント**: `QTreeView`
*   **機能**:
    *   ファイルシステムの階層表示。
    *   ドラッグ＆ドロップの受け入れ（移動/コピー）。
    *   「デスクトップ」「ドキュメント」「ダウンロード」等の標準フォルダへのショートカット表示（「クイックアクセス」的機能）。

#### C. 右ペイン：ファイル一覧ビュー
*   **コンポーネント**: `QListView` (アイコン表示) / `QTreeView` (詳細表示) の切替
*   **表示モード**:
    1.  **詳細表示 (List/Details)**:
        *   カラム: 名前, 更新日時, サイズ, 種類, (動画の場合) 再生時間, 解像度。
        *   ヘッダークリックによるソート機能。
    2.  **アイコン表示 (Grid/Tiles)**:
        *   大/中/小のサイズ変更対応。
        *   画像・動画ファイルのサムネイル表示。
*   **動画機能 (Smart Preview)**:
    *   マウスホバー時、対象が動画ファイルであればサムネイル領域内でダイジェスト再生（数フレームをパラパラ漫画的に切り替え）を行う。
    *   または、ホバー時にポップアップ等でプレビューを表示（実装負荷とUXを考慮し決定）。

#### D. フッター (ステータスバー)
*   **表示情報**:
    *   カレントディレクトリ内のアイテム数。
    *   選択されているアイテム数と合計サイズ。
    *   バックグラウンドタスク（サムネイル生成中など）の進捗状況。

### 3.2 ファイル操作機能 (Explorer準拠)
*   **基本操作**:
    *   新規フォルダ作成、新規ファイル作成。
    *   名前の変更 (F2)。
    *   削除 (Delete): OSのごみ箱へ移動 (winshell/send2trash利用)、Shift+Deleteで完全削除。
    *   プロパティ表示。
*   **クリップボード連携**:
    *   OS標準クリップボードを使用したコピー/カット/ペースト。
    *   エクスプローラーとの相互コピー＆ペースト対応。
*   **ドラッグ＆ドロップ**:
    *   アプリ内部での移動/コピー。
    *   外部アプリ（OS標準ファイラー等）との相互ドロップ対応。
*   **実行**:
    *   ダブルクリック: OSの関連付け設定に従いファイルを開く (`QDesktopServices.openUrl`)。
    *   右クリックメニュー: 「プログラムから開く」の実装。

### 3.3 動画・メディア管理要件
*   **サムネイル自動生成**:
    *   対応フォーマット: mp4, mkv, avi, mov, webm 等 (OpenCV依存)。
    *   実装: バックグラウンドスレッドでOpenCVを用いてフレームを抽出・縮小し、`QIcon` / `QPixmap` 化してキャッシュ。
    *   キャッシュ機構: 生成したサムネイルはメモリ内キャッシュおよびディスクキャッシュ（SQLite等が望ましい）に保存し、次回以降の読み込みを高速化。
*   **メタデータ解析**:
    *   動画ファイルのプロパティとして、Duration (再生時間), Width, Height, FPS 等を取得。
    *   詳細表示モードのカラムに追加表示。

## 4. 非機能要件
1.  **クロスプラットフォーム**: Windowsパス (`\`) と UNIXパス (`/`) の違いを `pathlib` 等で吸収し、単一コードベースで動作させる。
2.  **パフォーマンス**:
    *   ディレクトリ読み込み時の遅延ロード (FetchMore) 対応。
    *   1000件以上のファイルを含むフォルダでもGUIがフリーズしない構造。
3.  **エラーハンドリング**:
    *   アクセス権限がないフォルダへのアクセス時の例外処理。
    *   ファイル移動・削除時の競合・失敗時のユーザー通知。

## 5. データ構造・保存
*   **設定**: ユーザー設定（ウィンドウサイズ、表示モード、ソート順など）をJSON等で永続化。
*   **キャッシュ**: サムネイル画像や検索インデックスの保存場所を確保（OSごとの標準キャッシュディレクトリを推奨）。

## 6. 今後の拡張性 (Option)
*   タブ機能。
*   デュアルペインモード。
*   高度な検索インデックス作成。
