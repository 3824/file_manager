# GUIファイラー UI/UX 改善案

本ドキュメントは、GUIファイラーのUI改善案を現状の実装に合わせて整理したものです。
詳細な設計・実装履歴は `docs/ui_modernization_plan.md` を参照してください。

## 現状サマリ

すでに次の改善は実装済みです。

- Fluent / One Commander風のライト・ダークテーマ
- テーマトークンとQSSテンプレートの分離（`src/file_manager/ui_theme.py`）
- qtawesomeによるツールバー・ナビゲーションアイコン統一（`src/file_manager/ui_icons.py`）
- Windows 11 22H2+でのMicaバックドロップ試行
- システムアクセントカラー追従
- 設定ダイアログの「外観」タブ
- ルート余白・バー余白・ボタン高さなどの密度調整
- スクロールバーのホバー強調
- スプリッターのダブルクリック折りたたみ
- テーマ関連テスト（`tests/test_ui_theme.py`）

そのため、以下では「まだ追加・改善すると効果が高い項目」を中心に扱います。

---

## 1. 優先度高: 既存体験を壊さず効く改善

### 1.1 アドレスバーのパンくず化

現在のアドレスバーはパス文字列入力が中心です。クリック可能なセグメントに分割したパンくずUIを追加すると、上位フォルダへの移動が速くなります。

- `C:\Users\name\Videos` を `C:` / `Users` / `name` / `Videos` のように分割表示する
- 各セグメントをクリックで移動
- 末尾または空白部クリックで従来のテキスト入力に切り替え
- 長いパスは中間を省略し、ドロップダウンで展開

注意点:

- UNCパス、ドライブ直下、存在しない入力中パスを壊さない
- キーボード操作では従来通り直接入力できるようにする

### 1.2 タブUI

複数フォルダを行き来する操作が多いファイラーでは、タブは効果が大きいです。

- 右ペイン単位で現在パス・選択・ソート状態を保持
- 新規タブ、タブを閉じる、複製、右側を閉じる
- 起動時に前回タブを復元するかは設定で選択

注意点:

- 最初は1ペイン1タブで実装し、複数ペインやカラム表示とは分ける
- ファイル操作中にタブを閉じた場合の扱いを明確にする

### 1.3 ファイル操作の進行状況と取り消し

コピー・移動・削除などの状態が見えると、安心して操作できます。

- ステータスバーまたは下部パネルに進行中ジョブを表示
- 失敗ファイル、スキップ数、残り時間を表示
- 可能な処理はキャンセル対応
- ごみ箱移動と完全削除を視覚的に区別

注意点:

- UIスレッドをブロックしない
- 大量ファイル時のエラー集約表示を用意する

---

## 2. 優先度中: 視認性と操作効率の改善

### 2.1 ファイルリストの情報設計

現在のモダンテーマを活かしつつ、ファイル種別や状態をより読み取りやすくします。

- ファイル種別アイコンを一覧にも適用
- 隠し・読み取り専用・システム属性を細いカラーバーやバッジで表示
- 更新日時・サイズ列の配置と幅を見直し
- 選択件数、合計サイズ、現在ドライブ残量をステータスバーで常時表示

注意点:

- 色だけで状態を表現しない
- 行高を増やしすぎず、現在の高密度設計を維持する

### 2.2 コマンドパレット

メニューやツールバーにある機能が増えているため、キーボードから検索して実行できる入口を追加します。

- `Ctrl+Shift+P` などで起動
- 「検索」「重複動画」「ファイル名翻訳」「設定」などを絞り込み
- 最近使ったコマンドを上位表示

### 2.3 コンテキストメニューの整理

右クリックメニューは機能が増えるほど迷いやすくなります。

- 標準操作、分析、AI/翻訳、危険操作のグループに分ける
- 破壊的操作は色・アイコン・確認ダイアログで区別
- 選択状態に応じて不要な項目を非表示または無効化

---

## 3. 動画ファイラーとしての強化

### 3.1 ビデオウォール表示

動画ダイジェストやサムネイル機能を、一覧表示とは別のグリッド表示として強化します。

- 動画ファイルをカード状に並べる
- サムネイル、長さ、解像度、サイズ、更新日時を表示
- ホバーで動画ダイジェストを短くプレビュー
- ダイジェスト生成中・失敗時の状態表示

注意点:

- 初回表示で大量生成しない。表示範囲・選択分・明示操作を優先する
- キャッシュ容量と再生成条件を設定可能にする

### 3.2 動画重複・類似候補の比較UI

重複動画や類似ファイルの判断をしやすくします。

- 候補同士を左右比較
- サイズ、長さ、解像度、作成日時、ハッシュ結果を並べる
- 残す候補を明示し、削除候補を一括選択
- 実行前に最終確認リストを表示

### 3.3 AIクイックアクション

AI関連の操作は便利ですが、誤操作やコストが分かりにくい点があります。

- 右クリックメニューに「AI」グループを作る
- 実行前に対象数、推定処理時間、外部API利用有無を表示
- ファイル名推測・翻訳は必ずプレビュー画面を挟む
- APIキーなどは `.env` / 環境変数から読み、UIにはマスク表示する

---

## 4. アクセシビリティと品質

### 4.1 Reduce motion対応

現在は大きなアニメーションが少ないため影響は限定的ですが、今後タブ・サイドバー・プレビューを動かす場合に必要です。

- OSの「アニメーション効果」設定を検出
- 無効時はアニメーション時間を0にする
- 設定ダイアログにも「アニメーションを減らす」を用意

### 4.2 コントラスト検証

テーマトークンが増えているため、自動テストで最低限の視認性を守るべきです。

- 本文テキストはWCAG 4.5:1以上
- UI部品・境界・フォーカス表示は3:1以上
- ライト・ダーク・システムアクセントの代表色をテスト

### 4.3 スクリーンショット記録

UI改善はレビュー時に差分が分かりにくいため、手動でも記録を残します。

- ライト・ダークそれぞれのメイン画面
- 設定ダイアログ
- ファイル検索、動画ダイジェスト、重複候補画面
- 保存先は `docs/screenshots/`

---

## 5. 実装時の優先順

1. パンくずアドレスバー
2. ステータスバー情報の強化
3. ファイルリストの種別アイコン・属性表示
4. タブUI
5. コマンドパレット
6. ビデオウォール表示
7. 動画比較UI
8. Reduce motionとコントラスト自動テスト

## 6. 見送る・慎重に扱う案

- Acrylicを全ポップアップへ広く適用する案  
  Qt/QSSだけでは安定しづらく、まずはMica + 半透明風トークンで十分です。

- 強い角丸（12px以上）やカード多用  
  デスクトップファイラーでは情報密度を下げるため、基本は4pxから8pxに留めます。

- Bento Gridの全面採用  
  ホーム画面や分析画面には有効ですが、通常のファイル一覧には向きません。

- AI機能を目立たせすぎる案  
  ファイラーの主操作はローカルファイル管理です。AIは補助機能として、実行前プレビューと安全確認を優先します。

---

## 7. 参照

- `docs/ui_modernization_plan.md`: UI刷新の詳細設計と実装状況
- `src/file_manager/ui_theme.py`: テーマトークン・QSS
- `src/file_manager/ui_icons.py`: アイコン管理
- `tests/test_ui_theme.py`: テーマ関連テスト
